import Link from 'next/link';

export default function CookiesPage() {
  return <main className="min-h-screen bg-background px-6 py-16 text-primary"><article className="mx-auto max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-brand">Cookie notice · 12 September 2026</p><h1 className="mt-3 text-4xl font-bold">Cookies and local storage</h1><div className="mt-8 space-y-7 text-sm leading-7 text-secondary"><p>Attention Token uses a secure, HTTP-only authentication cookie to keep a signed-in wallet session active. The landing page uses local storage for splash-screen state and referral, invitation, and campaign attribution until account creation.</p><p>Vercel Web Analytics is configured without wallet addresses or financial identifiers. The embedded Offerwall.GG wall states that it sets no cookies; it still receives device and network information required for targeting and fraud prevention.</p><p>Questions can be sent to <a className="text-brand" href="mailto:support@attentiontoken.net">support@attentiontoken.net</a>.</p></div><Link href="/" className="mt-10 inline-flex text-sm font-semibold text-brand">← Return home</Link></article></main>;
}
